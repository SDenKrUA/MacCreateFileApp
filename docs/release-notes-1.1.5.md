# Mac Create File 1.1.5

## Fixed

- Fixed Finder menu icons staying black in dark mode.
- Kept the newer SF Symbol menu icon shapes while rendering them as tinted non-template images before Finder displays them.
- Kept the Finder toolbar icon as a template SF Symbol so Finder can tint it correctly in light and dark mode.
- The release artifact remains a `.dmg` installer image: `MacCreateFile-1.1.5-mac-arm64.dmg`.
