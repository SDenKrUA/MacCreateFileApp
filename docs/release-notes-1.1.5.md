# Mac Create File 1.1.5

## Fixed

- Fixed Finder menu and toolbar icons staying black in dark mode.
- Kept the newer SF Symbol icon shapes while rendering them as tinted non-template images before Finder displays them.
- The release artifact remains a `.dmg` installer image: `MacCreateFile-1.1.5-mac-arm64.dmg`.
