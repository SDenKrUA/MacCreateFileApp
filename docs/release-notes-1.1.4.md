# Mac Create File 1.1.4

## Changed

- Restyled the `Mac Create File Extension` and `Show in Dock` toggles into one compact system-style settings group.
- Removed redundant `Enabled` / `Disabled` labels under the toggle titles.
- Reduced the main window height after compacting the toggle rows.
- The release artifact remains a `.dmg` installer image: `MacCreateFile-1.1.4-mac-arm64.dmg`.
