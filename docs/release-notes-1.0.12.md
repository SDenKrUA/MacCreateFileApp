# Mac Create File 1.0.12

## Added

- A generated macOS app icon showing a document with a blue plus badge.
- A Finder menu icon for the top-level `Create File` item using `doc.badge.plus`.

## Changed

- The build script now generates `MacCreateFileAppIcon.icns` automatically during packaging.

